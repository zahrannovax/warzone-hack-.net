---
import { getLocalizedPath } from '../data/i18n/routing';
import { defaultLocale, type LocaleCode } from '../data/i18n/locales';
import { getBlogBasePath } from '../data/blog/helpers';
import { getT } from '../i18n/server';

interface Props {
	locale?: LocaleCode;
}

const locale = Astro.props.locale ?? defaultLocale;
const t = getT(locale);

const categories = [
	{ label: t('categoryRow.hacks'), href: getLocalizedPath('hacks', locale) },
	{ label: t('categoryRow.esp'), href: getLocalizedPath('warzone-esp', locale) },
	{ label: t('categoryRow.aimbot'), href: getLocalizedPath('warzone-aimbot', locale) },
	{ label: t('categoryRow.radar'), href: getLocalizedPath('radar', locale) },
	{ label: t('categoryRow.wallhack'), href: getLocalizedPath('wallhack', locale) },
	{ label: t('categoryRow.undetected'), href: getLocalizedPath('undetected', locale) },
	{ label: t('categoryRow.pricing'), href: getLocalizedPath('pricing', locale) },
	{ label: t('categoryRow.setup'), href: getLocalizedPath('setup', locale) },
	{ label: t('categoryRow.blog'), href: getBlogBasePath(locale) },
];
---

<nav class="cats" aria-label={t('common.browse')}>
	<div class="shell">
		<ul class="cats__list">
			{
				categories.map((cat) => (
					<li>
						<a href={cat.href}>{cat.label}</a>
					</li>
				))
			}
		</ul>
	</div>
</nav>

<style>
	.cats {
		border-block: 1px solid var(--line-soft);
		background: var(--bg-panel);
	}

	.cats__list {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 0.5rem;
		margin: 0;
		padding: 1rem 0;
		list-style: none;
	}

	.cats__list a {
		display: inline-flex;
		align-items: center;
		padding: 0.4rem 0.8rem;
		border: 1px solid var(--line);
		border-radius: var(--radius-pill);
		background: var(--bg-elevated);
		font-size: 0.8125rem;
		font-weight: 500;
		color: var(--ink-muted);
		transition: color 0.15s ease, border-color 0.15s ease, background 0.15s ease;
	}

	.cats__list a:hover {
		color: var(--ink);
		border-color: color-mix(in srgb, var(--magenta) 45%, var(--line));
		background: color-mix(in srgb, var(--magenta) 10%, var(--bg-elevated));
	}

	@media (max-width: 720px) {
		.cats__list {
			flex-wrap: nowrap;
			overflow-x: auto;
			scrollbar-width: none;
			margin-inline: calc(var(--page-gutter) * -1);
			padding-inline: var(--page-gutter);
			padding-bottom: 0.15rem;
			-webkit-overflow-scrolling: touch;
		}

		.cats__list::-webkit-scrollbar {
			display: none;
		}

		.cats__list a {
			white-space: nowrap;
			flex-shrink: 0;
			min-height: var(--touch-min, 2.75rem);
			padding-inline: 1rem;
		}
	}
</style>
